# Railway: set the service root directory to `problem-5` in the dashboard
# (or deploy only this folder as the repo root).

[build]
builder = "NIXPACKS"

[deploy]
startCommand = "node dist/server.js"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
